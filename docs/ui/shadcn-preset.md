# shadcn Preset

Current stored UI metadata:

- style: `radix-vega`
- preset reference: `aIkf1TW`
- menu treatment: `default-translucent`
- menu accent: `subtle`
- Tailwind class prefix: empty string
- generated alias set includes `@/hooks`

To bootstrap the same preset shape:

```bash
bunx --bun shadcn@latest init --preset aIkf1TW
```

Relevant config lives in `components.json` and `src/globals.css`.
